import { styled } from '@mui/system';
import { Button } from "@mui/material";

const PrimaryButton = styled(Button)({
    background: 'linear-gradient(90deg, #2CC0FF, #0A1B22)',
    color: '#FFF',
    fontWeight: '700',
    padding: '10px 15px'
})

export default PrimaryButton;